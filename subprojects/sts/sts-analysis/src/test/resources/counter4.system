specification Counter4 {
    property safe: {
        var x: int
        invariant x >= 0

        initial x = 0

        transition if x < 5 then x' = x + 1 else x' = 2

    } models G( x = 1 or x = 3)
}